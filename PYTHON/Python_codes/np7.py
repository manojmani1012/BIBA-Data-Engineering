#NymPy ufuncs
import numpy as np

a = np.array([0, 2, 3, 4]) 
print ("Exponent:", np.exp(a))                # exponential values 

print ("Square root :", np.sqrt(a))             # square root 

b = np.array([0, np.pi/2, np.pi])                       # create an array of sine values 
print ("Sine values elements:", np.sin(b)) 
  
